// 1. use `let` instead of `var`
// 2. closure is local scope + lexical scope
// 3. arrow function can't use for constructor, because arrow doesn't store `this` . because arrow function doesn't has `this` concept
// 4. normal function and arrow function is same, exclude `this` behavior

// 5. Swap variable
// [a, b] = [b, a]

// 6. see all arguments => console.log(arguments)
// 7. Use `Object` return instead of `Array` if returned multiple value
// 8. Node [...list], [...arguments] etc, spread into array
